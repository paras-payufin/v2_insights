You are a senior data scientist specialising in model monitoring and evaluation.

Analyse the attached HTML file using only values contained in that file. Produce one self-contained, production-grade HTML monitoring report for the Uptop V3 model at the Bajaj Credit Check stage.

The final response must contain only raw HTML:
first characters: <!DOCTYPE html>
last characters: </html>

Do not provide commentary, Markdown, explanations, or code fences.

WORKFLOW

1. Parse the HTML tables in document order using their headings, captions, column headers, and schemas. Do not rely on assumed physical table numbers.
2. Build an internal data ledger before writing HTML:
   - reporting months and report date
   - Credit Check status counts and percentages
   - Credit Check and Disbursed score buckets and PSI
   - Credit Check and Disbursed risk segments
   - Credit Check and Disbursed approval methods
   - Credit Check and Disbursed Bureau Depth segments
   - Credit Check and Disbursed feature CSI values
   - Disbursal counts, amounts, and amount_cr
3. Treat the “All” column as an aggregate, never as a reporting month.
4. Preserve every source value and label exactly as written. Do not rename, round, infer, estimate, or reformat source values.
5. Use only the attached file. Do not use external knowledge, URLs, assumptions, benchmarks, or business explanations not supported by the file.
6. Complete all calculations and validation checks before generating the report.
7. Every insight must be evidence-based and tied to a source value or an explicitly calculated comparison. Do not claim causality.
8. If data is missing, ambiguous, or denominators are not comparable, display “—”. Do not estimate or substitute values.
9. Keep the following nine sections in exactly this order and use the exact headings.

CALCULATIONS

A. Pipeline rates

For the latest and prior reporting months, calculate from the Credit Check status count table:

DR = Disbursed / All × 100
RR = Rejected / All × 100
CA = CA But LPS Not Done / All × 100

Calculate MoM deltas from full-precision rates:

DR_delta = DR_latest − DR_prior
RR_delta = RR_latest − RR_prior

Round only the final deltas to 2 decimal places. Never subtract displayed rounded percentages.

B. Average ticket

For each month:

Avg Ticket (₹ L) = amount_cr × 100 / size

Use 1 Crore = 100 Lakhs. Display to 2 decimal places.

Validate:

Avg Ticket × size / 100 = amount_cr ± 0.05

If any result exceeds ₹5.00 L or fails validation, recompute before continuing.

C. CSI classification

For every Disbursed feature CSI value in the feature stability table:

> 0.25       = Unstable
0.10–0.25    = Marginal
< 0.10       = Stable

Exactly 0.25 is Marginal. Count U, M, and S and verify:

U + M + S = total Disbursed feature rows

Use the latest month for current classification. Use every available month independently for instability streaks.

D. Top three

Rank the latest-month Disbursed CSI values from highest to lowest. Select the three largest values, regardless of row order.

E. RAG rules

Disbursed PSI:
< 0.10 = Green
0.10–0.20 = Amber
> 0.20 = Red

CA-LPS percentage:
< 15 = Green
15–30 = Amber
> 30 = Red

Disbursal-rate drop:
drop = DR_prior − DR_latest using unrounded rates
≤ 3pp = Green
> 3pp and ≤ 5pp = Amber
> 5pp = Red

Rejection-rate badge:
increase > 3pp = Amber
decrease or increase ≤ 3pp = Green

Feature CSI:
U > 0 = Red
U = 0 and M > 0 = Amber
U = 0 and M = 0 = Green

Overall RAG is the worst applicable RAG among Disbursed PSI, Disbursed feature CSI, and funnel RAGs. Exclude Credit Check PSI and Credit Check CSI entirely.

SOURCE AND DENOMINATOR RULES

- Bind each value to its own table and population.
- PSI values come only from PSI tables.
- CSI values come only from feature CSI tables.
- Pipeline rates come only from the Credit Check status count table.
- Disbursal amount and volume come only from the Disbursal Data table.
- Do not migrate values across tables.
- Verify numerator and denominator belong to the same table before calculating.
- If a table has a local “All” denominator different from the overall Credit Check total, use the table-local denominator for that table and do not describe it as the overall Credit Check population.
- If a category is absent from a comparison table, treat it as absent, not as an assumed zero. Display “—”; display 0.00% only where the source explicitly provides zero.
- Risk-segment presence is exact-string matching only. Similar labels are different segments.
- Use source labels verbatim for all segments, buckets, methods, statuses, and features.
- CC PSI is “Informational” with no alert or RAG badge.
- CC CSI is reference-only with no badge, colour, alert, or commentary on its level.
- Disbursed CSI > 0.25 is always “Critical”.
- Approval method with Credit Check > 20% and Disbursed < 5% is always “Critical”.
- A persistent CSI condition means at least two consecutive months with CSI > 0.25. A month at or below 0.25 resets the streak.

INSIGHT RULES

- Describe only observable direction, magnitude, comparison, or threshold breach.
- Do not infer causes, losses, revenue, market size, customer behaviour, model performance, or operational impact unless directly supported by the file.
- Do not call a value “improving” or “worsening” unless the relevant metric changed in that direction.
- If a comparison is not valid because populations or denominators differ, state that within the relevant narrative and do not calculate a misleading gap.
- Do not name individual features in the Section 3 health narrative.
- Mention only confirmed absent segments or buckets.
- Use “verify source data” only for anomalous source values; never correct them.

OUTPUT CONTRACT

SECTION 1 — REPORT HEADER
Title: “UpTop V3 — Model Monitoring Report”
Sub-line: “Bajaj Credit Check | Live Pipeline Data”
Show Reporting Month, Report Date, and Overall RAG.

SECTION 2 — MODEL OVERVIEW
Two-column cards containing model name, purpose, risk-segment logic, cadence, data source, months available, latest month, total Credit Check applications, total disbursals, and the Bureau Depth definition.

SECTION 3 — KPI DASHBOARD
Seven cards in this order:
1. PSI (Credit Check)
2. PSI (Disbursed)
3. Disbursal Rate
4. Rejection Rate
5. CA But LPS Not Done %
6. Avg Ticket Size (₹ L)
7. Feature CSI (Disbursed)

Include the required RAG or Informational badges and a 2–3 sentence health narrative.

SECTION 4 — APPLICATION PIPELINE FUNNEL
Show Status, prior/latest counts and percentages, and MoM delta in percentage points for:
Disbursed, Rejected, CA But LPS Not Done, Pre-approved, Expired, Open, Cancelled, Closed.

Include required alerts, trend narrative, and notable shifts above 5pp.

SECTION 5 — PSI & SCORE DISTRIBUTION
Show all-month PSI history for Credit Check and Disbursed populations, CSS-only PSI bars, current Disbursed PSI flags, and the latest-month score-bucket comparison with exact labels.

SECTION 6 — CSI & FEATURE STABILITY
Show Feature Name, CC CSI, CC Status, Disbursed CSI, Disbursed Status, and MoM Trend. Include row classification, adverse-selection marker, Top 3 panel, instability streaks, persistence flags, and the required narrative.

SECTION 7 — DISBURSAL, RISK SEGMENT & BUREAU DEPTH ANALYSIS
Include:
A. Disbursal Trend
B. Risk Segment Distribution
C. Approval Method Distribution
D. Bureau Depth Segment Distribution

Use the required columns, exact labels, calculations, flags, and narratives.

SECTION 8 — EARLY WARNING ALERTS
Header: “Active Alerts”
Sub-line: “Evaluated: [report date from file]”

Include one row per triggered metric or feature, with:
#, Metric, Current Value, Threshold, Status, Recommended Action.

Apply all specified Critical, Warning, Persistent, and exclusion rules. If none are triggered, write:
“No active alerts — all metrics within acceptable range.”

SECTION 9 — SUMMARY & ACTIONABLE INSIGHTS
Include 4–6 evidence-based bullet findings using file values only.

Include the action table:
Finding | Recommended Action | Owner | Priority | Timeline

Owner mapping:
PSI/CSI = Data Science
funnel/pipeline = Operations + Product
disbursal/conversion = Business + Analytics
segment/method = Risk + Product

Priority mapping:
Critical = P0 (Week 1)
Warning = P1 (Week 2–3)
Observational = P2 (Week 3–4)

Close with:
“Next Review Date: [2 weeks from the last day of the latest reporting month]”

HTML REQUIREMENTS

- One HTML file only.
- Inline CSS in a single <style> block.
- No JavaScript.
- No external stylesheets, scripts, fonts, images, libraries, or CDNs.
- Full-width layout with 32px side padding.
- All tables width: 100%.
- Use the specified navy, green, amber, red, grey, card, table, grid, and CSS-only bar-chart styling.
- Do not add sections, appendices, disclaimers, placeholders, or AI notices.