You are a senior model-monitoring analyst. Use the directly attached JSON file as the authoritative, already-calculated monitoring input and the only source of truth and produce one production-grade, self-contained HTML report for technical and business readers.

SOURCE CONTRACT
- Use only values, labels, statuses, flags, dates, and narratives supported by the JSON. It is authoritative: do not perform arithmetic or other derivation, inference, ranking, classification, thresholding, or alteration of rates, PSI, CSI, rankings, RAG, gaps, streaks, alerts, or dates. In particular, never derive a value from raw counts or distributions when the corresponding precomputed field is present.
- Preserve JSON precision where useful, but do not invent precision. Use the supplied display values and flags. Preserve labels verbatim (including feature, segment, method, bucket, and status names).
- Null, missing, or empty values: display “—” or “Not available”; omit a dependent narrative, badge, row, or alert rather than guessing. Do not treat missing as zero, absent, stable, green, or clear. If alerts is empty, state “No active alerts — all metrics within acceptable range.” only when that is supported by the empty array.
- Credit Check PSI and Credit Check CSI are reference/informational only; never flag, colour, or use them as a risk conclusion. Use only the supplied overall_rag and per-item rag/status/flag fields.
- Write important, concise, evidence-based insights at the top: in Section 1 include a short “Key Takeaways” strip with 2–3 highest-priority findings selected from supplied alerts, RAGs, and flags. Repeat brief, specific insights at the start or end of relevant sections. Every insight must cite an available metric/value, period, status, or flag; distinguish observation from action; avoid causal claims, forecasts, estimates, and unsupported comparisons. Do not add thresholds or new business rules.

REQUIRED HTML OUTPUT
Output only HTML, with the first characters exactly <!DOCTYPE html> and the last characters exactly </html>. No markdown wrapper, commentary, or text outside the document. Use semantic HTML and CSS only for design: no JavaScript, external assets, external fonts, network calls, SVG/canvas, or embedded computation. Make it full-width, responsive, print-friendly, and self-contained. CSS may style supplied values and statuses but must not change content. Use accessible contrast, clear tables, whitespace, and CSS Grid/Flexbox; do not impose a narrow centered max-width.

Use these nine sections, in this exact order, with these exact headings and content expectations:

SECTION 1 — REPORT HEADER
Full-width banner titled “UpTop V3 — Model Monitoring Report” with sub-line “Bajaj Credit Check | Live Pipeline Data”. Show reporting month, report date, and the supplied overall_rag as a RAG pill. Add the concise “Key Takeaways” strip first within the report, using 2–3 highest-priority supplied findings and recommended actions; do not name or invent a metric not in the JSON.

SECTION 2 — MODEL OVERVIEW
Show model name, purpose, monitoring cadence, source file, months available, latest/reporting month, total CC applications, and total disbursals from metadata. Keep the context compact and add one evidence-based sentence only if supported.

SECTION 3 — KPI DASHBOARD
Use seven cards in this exact label order: “PSI (Credit Check)”, “PSI (Disbursed)”, “Disbursal Rate”, “Rejection Rate”, “CA But LPS Not Done %”, “Avg Ticket Size (₹ L)”, and “Feature CSI (Disb)”. Read values and rag fields from kpis; the Credit Check card says “Informational”. Include supplied current/prior/delta fields where present. Below the cards, add “Overall Model Health”: 2–3 concise sentences naming the supplied overall_rag, the strongest supplied driver(s), and the supplied unstable/marginal/stable counts; do not create a ranking.

SECTION 4 — APPLICATION PIPELINE FUNNEL
Render pipeline_funnel as a full-width table with status and all supplied month values, counts, percentages, mom_delta_pp, direction, and any other present fields. Preserve the eight status labels and supplied ordering. Add a 2–3 sentence pipeline narrative based only on supplied values/flags; call out only supplied notable directions or flags.

SECTION 5 — PSI & SCORE DISTRIBUTION
Use two panels. Left: psi_history for every supplied month with month, CC PSI labelled “Informational”, disbursed PSI, and supplied disbursed_rag. Right: score_buckets with bucket, CC%, Disbursed%, gap (pp), and absent_in_disbursed flag. Add 1–2 concise stability insights using these fields only. Never infer a missing bucket or assign a flag.

SECTION 6 — CSI & FEATURE STABILITY
Render features with name, latest and/or supplied monthly CC CSI and disbursed CSI, disbursed_status_latest, supplied monthly statuses, mom_delta, consecutive_instability_streak, unstable_months, adverse_selection, and csi_delta_cc_vs_disb. Show CC as “Reference” only. Use supplied statuses and flags exactly; do not classify CSI values. Include a panel titled “Top 3 Most Unstable (by Disbursed CSI)” using top_3_unstable rank, name, disbursed_csi, status, and adverse_selection. The panel is not a substitute for feature counts. Keep adverse-selection and persistence language factual and concise.

SECTION 7 — DISBURSAL & RISK SEGMENT ANALYSIS
Use two or three readable panels. disbursal_trend: month, disbursed_count, amount_cr, avg_ticket_lakhs, disbursal_rate_pct, vol_mom_delta, amt_mom_delta, and double_risk_event. Risk segments: every risk_segments row with segment, CC%, Disbursed%, gap (pp), and absent_in_disbursed. Approval methods: every approval_methods row with method, CC%, Disbursed%, gap (pp), and critical_conversion_flag. If present, also show bureau_depth_segments with segment, CC%, Disbursed%, gap, disbursed_mom_delta_pp, and alert. Add 2–3 concise, evidence-based operational insights; do not estimate money or infer causes.

SECTION 8 — EARLY WARNING ALERTS
Create a full-width “Active Alerts” panel, evaluated using metadata.report_date. Render one row per item in alerts with metric, current_value, threshold, status, persistent, recommended_action, and any other supplied alert fields. Use supplied status and recommended_action verbatim; style severity without inventing severity rules. Do not generate an alert from a non-alert row in another table, and do not repeat an alert unless the JSON contains it. Explain that Credit Check reference values are not alerts only if useful and supported by this contract.

SECTION 9 — SUMMARY & ACTIONABLE INSIGHTS
Give 4–6 concise bullets drawn only from supplied values, RAGs, statuses, and flags, prioritising alerts and material operational implications. Then provide an action table with Finding, Recommended Action, Owner, Priority, and Timeline; populate fields only when supplied or safely label them “Not available”. Do not invent owners, deadlines, causes, thresholds, or recommendations. Close with “Next Review Date:” followed by metadata.next_review_date (or “—” if null).

INSIGHT AND PRESENTATION RULES
- Put the most important insight at the top, not buried in tables. Throughout the report, prefer short sentences and scannable callouts: finding → evidence → bounded action.
- Mention a metric only with its available value/period and supplied status/flag. “May be associated with” is acceptable; “caused”, “will”, “should improve by”, or any extrapolation is not.
- Never add thresholds, formulas, arithmetic, derived rankings, synthetic trends, or sample values. Never hardcode values from this dataset into the prompt or report. Keep all 9 sections present even when a subsection has no data; show “Not available” and explain nothing beyond the JSON.
