You are a senior data scientist specializing in real-time fraud model monitoring. Analyze the attached Fraud Model Monitoring Report (Excel file) and produce a structured insights summary. Output plain text only — no HTML, no code blocks.

Context:
- Product: Lazypay fraud ML model (transaction-level decline / holdout scoring)
- Merchants: Myntra, Blinkit, Zomato, BMS, District Dining
- Cadence: biweekly (1st-15th / 16th-EOM) + month-end snapshots from 1 May 2025
- TEST = model actively declining; HOLDOUT = scores observed, no declines
- Green = score < cutoff (healthy); Red = score >= cutoff (breach)
- Use ONLY values present in the attached report. Where data is missing, show "—" and note it.

Produce EXACTLY these 8 sections using EXACTLY these bold headers. Start DIRECTLY with the first header — zero words before it.

**Report Header:**
State: "Fraud Model Monitoring Report — Lazypay | Live Pipeline Data". Include the latest biweekly period from the data, monitoring start "1st May'25 onwards", overall RAG status (Green/Amber/Red — derive from worst merchant + PSI/CSI), merchant count 5.

**Executive Summary:**
One row per merchant (Myntra, Blinkit, Zomato, BMS, District Dining) plus portfolio total. For each state: Cumulative Frauds, Holdout FTS, Test FTS after declines, % Reduction ((Holdout FTS - Test FTS)/Holdout FTS), % Capture from Holdout, % GMV Declined, Precision — with current value, change vs prior period (up/down), and RAG status. Follow with 3-5 sentences: overall trend, best/worst merchant, whether declines are holding.

**Parameters:**
List with current values and one-line business impact: PSI warning 0.10, PSI critical 0.20, CSI warning 0.10, CSI critical 0.25, model score cutoff (per merchant), GMV decline tolerance, capture rate floor, precision floor. Flag any that are currently breached.

**Monthly Performance:**
Per merchant (Myntra, Blinkit, Zomato, BMS): month-end values for Frauds, Holdout FTS, Test FTS after declines, % Reduction, % Capture, % GMV Declined, Precision. Add 2-3 sentences per merchant on trend direction and whether latest month is better or worse. Flag NA precision with reason.

**PSI & CSI Stability:**
PSI: monthly values with RAG (<=0.10 Green / 0.10-0.20 Amber / >0.20 Red). 2-3 sentences on drift trend and recommended action for any amber/red.
CSI: top features with monthly values and RAG (<=0.10 Green / 0.10-0.25 Amber / >0.25 Red). Call out features in red for 2+ consecutive months (high priority) and top 3 most unstable in the latest month.

**Merchant Deep-Dive:**
Per merchant — extract values for: Volume (days, txn counts/amounts, fraud counts), ML decline impact (count rate %, amount rate %), TEST block (GMV share, fraud rates), HOLDOUT block (GMV share, fraud rates), decline effectiveness vs test, holdout precision. Add 3-5 sentences: Test vs Holdout FTS trend, GMV Declined vs tolerance, decline count rate biweek-on-biweek.

**Early Warning Alerts:**
For each alert state: metric | merchant | current value | threshold | status (Critical/Warning/OK) | action:
- PSI drift (>0.10 Warning / >0.20 Critical)
- CSI feature drift (>0.25 or >0.10 for 2+ months)
- Holdout fraud rate breach (rate >= cutoff)
- Test fraud rate breach (rate >= cutoff)
- FTS deterioration (Test FTS rose vs prior)
- GMV spike (% GMV Declined exceeded tolerance)
- Capture rate drop (% capture fell below floor)
- Precision collapse (precision dropped below floor or NA)
- Decline rate spike (ML decline count rate jumped biweek-on-biweek)

**Summary & Actions:**
3-5 bullet findings with specific numbers (e.g. "Blinkit capture 11% vs 18% prior — At Risk — threshold breach"). Then action table: Finding | Recommended Action | Owner | Priority | Timeline. Close with next review date (next 1st or 16th of month).
