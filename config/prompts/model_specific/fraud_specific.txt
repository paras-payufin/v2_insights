Role: Senior fraud model monitoring analyst. Read the supplied raw Fraud Model Monitoring Report (Excel file) and produce a polished, production-ready executive summary HTML. Use ONLY metrics present in the input. Do NOT invent data. Where data is missing (e.g. NA, blank), show "—".

Output: A single self-contained HTML file — all CSS in a <style> block, Chart.js via CDN only external dependency. Label all data as "Live Pipeline Data".

Product context:
- Lazypay fraud ML model (transaction-level decline / holdout scoring)
- Merchants: Myntra, Blinkit, Zomato, BMS, District Dining
- Cadence: biweekly (1st–15th / 16th–EOM) + month-end snapshots from 1 May 2025
- TEST population = model actively declining; HOLDOUT = scores observed, no declines
- Green = score < cutoff (healthy); Red = score ≥ cutoff (breach)

Sections to produce:

1. Header — title "Fraud Model Monitoring Report", subtitle "Lazypay", latest biweekly period, generated date, overall RAG status badge (derived from worst merchant + PSI/CSI breaches), merchant count chip

2. Executive Summary — per-merchant KPI cards (one row per merchant + portfolio rollup):
   Cumulative Frauds | Holdout FTS (lt+gt cutoff) | Test FTS after declines | % Reduction | % Capture from Holdout | % GMV Declined | Precision
   Each KPI: current value, delta vs prior period (▲/▼), RAG status. Follow with 3–5 sentence narrative.

3. Parameters Panel — styled card: PSI warning 0.10, PSI critical 0.20, CSI warning 0.10, CSI critical 0.25, model score cutoff (per merchant), FTS/GMV/capture/precision tolerances, periods to chart (10), merchants filter (all 5). Show name, value, description, impact.

4. Monthly Performance — one card per merchant (month-end columns): Frauds | Holdout FTS | Test FTS after declines | % Reduction | % Capture | % GMV Declined | Precision. Latest column highlighted. Line chart: Test FTS trend across months (all merchants grouped).

5. PSI table (monthly, RAG: ≤0.10 green / 0.10–0.20 amber / >0.20 red) + line chart with threshold bands. CSI top-features table (top 10–15 features × monthly values, same RAG scale) + horizontal bar chart for latest month. Do NOT use AUC, KS, Gini, DPD unless present in input.

6. Biweekly Deep-dive — one card per merchant with full metric table:
   - Rows 0–9: Volume & fraud base (Days, Txn Attempt/Successful Count+Amount, Fraud Count+Amount, ATS, Fraud/Success rate)
   - Rows 10–13: ML decline impact (count, amount, count rate %, amount rate %)
   - Rows 14–21: TEST block (light blue #e6f3ff) — GMV, share, fraud amount/count/rates
   - Rows 22–29: HOLDOUT block (orange #ffe7cc) — GMV, share, fraud amount/count/rates
   - Rows 30–33: Decline vs Test (light blue, cutoff-coloured) — count/amount, rates wrt Test
   - Rows 34–38: Holdout precision block (orange, cutoff-coloured) — count/share, fraud count, rate, precision
   Green text = score < cutoff; Red text = score ≥ cutoff. Latest biweekly column highlighted. Sticky first column + sticky header. Per merchant: 3 mini Chart.js charts — Test/Holdout FTS over time, % GMV Declined, ML decline count rate.

7. Early Warning Alerts — panel with severity (Critical / Warning / OK):
   PSI drift, CSI feature drift, Holdout fraud rate breach, Test fraud rate breach, FTS deterioration, GMV spike, Capture rate drop, Precision collapse, Decline rate spike.
   Each alert: metric, merchant, current value, threshold, recommended action, timestamp.

8. Summary & Actions — 3–5 bullet findings + prioritized action table (Finding | Recommended Action | Owner | Priority | Timeline). Next review date. Footer: team name, version, confidentiality note.

Design:
- Navy header (#0f172a → #1e293b gradient)
- White cards, subtle shadow, 12px border-radius
- RAG: green #2ecc71 / amber #f39c12 / red #e74c3c / accent #3498db
- TEST tint #e6f3ff | HOLDOUT tint #ffe7cc
- System font: -apple-system, Segoe UI, Roboto, Arial
- Sticky left nav: Executive Summary | Monthly Metrics | PSI/CSI | Merchant Deep-dives | Alerts | Actions
- Alternating table rows; latest column highlighted; sticky colour legend bar above biweekly tables
- Responsive (desktop + tablet) + @media print rules
- Define FTS, TEST, HOLDOUT on first use

START your output immediately with <!DOCTYPE html> — no preamble, no explanation, no acknowledgment before the HTML.
