HTML envelope: Output only HTML beginning with <!DOCTYPE html> and ending with </html>.

File-only truth: Every raw value and label must come from the attached file. No external knowledge, estimates, invented values, or causal claims.

Missing-data rule: Missing, ambiguous, or non-comparable values must be —. Never fill, carry forward, or convert missing values into zero.

Table binding: Use the nearest heading and table schema to identify each table. Never rely on assumed physical table numbers.

Population integrity: Never combine values from different tables or populations. Use the local table denominator when it differs from the overall population.

Raw-value preservation: Copy source values exactly. Only calculated metrics may be formatted according to the calculation rules.

Calculation precision: Use full precision internally. Round only final MoM deltas to 2 decimal places and Avg Ticket to 2 decimal places.

Rate validation: Confirm numerator and denominator are from the same table before calculating any rate or percentage.

CSI boundaries: >0.25 Unstable, 0.10–0.25 Marginal, <0.10 Stable. Exactly 0.25 is Marginal.

Severity rules: Disbursed CSI above 0.25 and approval methods meeting both threshold conditions are always Critical.

Excluded metrics: Credit Check PSI is Informational. Credit Check CSI is reference-only. Neither may create an alert or affect Overall RAG.

Exact matching: Segment, bucket, feature, method, and status names must match source strings exactly. Similar labels must never be merged.

Evidence-based narrative: Every insight must describe only a measured value, calculated change, threshold breach, or direct table comparison.

Final validation: Before output, verify all nine sections, all required rows, CSI count reconciliation, Top 3 ranking, absent-category checks, average-ticket sanity checks, RAG rules, and absence of external dependencies.