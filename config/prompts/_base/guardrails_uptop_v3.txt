FINAL GUARDRAILS

1. Use only the validated ledger and attached report. Never invent or estimate.
2. Preserve all source labels and raw values exactly.
3. Null, missing, ambiguous, or unavailable data = “—”.
4. Never mix values across tables or populations; “All” is not a month.
5. Use unrounded values for calculations and final rounded deltas only.
6. CSI: >0.25 Unstable; 0.10–0.25 Marginal; <0.10 Stable.
7. Credit Check PSI and Credit Check CSI are informational/reference-only and never create alerts or affect Overall RAG.
8. Use exact-string matching for absent buckets and segments.
9. Every insight must be directly supported by a value, comparison, trend, ranking, or threshold breach.
10. Render exactly nine sections in the required order.
11. Output only self-contained HTML, beginning with <!DOCTYPE html> and ending with </html>.
12. No JavaScript, external dependencies, Markdown, commentary, or reasoning.
