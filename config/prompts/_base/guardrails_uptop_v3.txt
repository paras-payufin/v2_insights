GUARDRAILS — JSON-DRIVEN REPORT (V1)

1. SOURCE LOCK
Use the directly attached JSON file as the only source of truth; it is the supplied, authoritative JSON input. It is already calculated: never recalculate, derive, round, normalize, reinterpret, correct, or alter any precomputed value. Do not merge values across sections. Preserve every raw JSON label, status, number, precision, and display value exactly. null, missing, or unavailable values render as —. Never invent insights, thresholds, causes, owners, financial impact, dates, actions, or explanations; causal claims are forbidden unless explicitly present in JSON.

Use these top-level keys exactly and only for their corresponding content:
overall_rag, kpis, pipeline_funnel, psi_history, score_buckets, features, top_3_unstable, disbursal_trend, risk_segments, approval_methods, bureau_depth_segments, alerts.

2. OUTPUT CONTRACT
Output raw HTML only as the chat/message body — never as a downloadable file, saved artifact, or attachment.
Write the complete HTML source code directly in your response.
Do NOT: generate a file, save an attachment, return a filename, or say "report generated successfully".
The first characters of your response must be <!DOCTYPE html> and the final characters must be </html>.
No text before or after, no markdown or code fences, no external dependencies, no CDN, no external stylesheet, no images, and no JavaScript.
Put CSS in an inline <style> block; use CSS only. The report must render offline and use a full-width layout: viewport-spanning wrapper, padding for margins, width:100% tables, and CSS Grid with fr units (no narrow centered column or restrictive max-width).

3. NINE SECTIONS — EXACT ORDER
Render all nine sections, in this order, without merging, skipping, renaming, adding, appendices, disclaimers, or placeholders:
1) REPORT HEADER
2) MODEL OVERVIEW
3) KPI DASHBOARD
4) APPLICATION PIPELINE FUNNEL
5) PSI & SCORE DISTRIBUTION
6) CSI & FEATURE STABILITY
7) DISBURSAL & RISK SEGMENT ANALYSIS
8) EARLY WARNING ALERTS
9) SUMMARY & ACTIONABLE INSIGHTS
Use the JSON values directly in the section to which they belong. Never move a value between tables or sections.

4. EXACT DATA MAPPING AND EXCLUSIONS
- Header/overview/KPI overall status: overall_rag and the supplied metadata only when present in the input.
- KPI dashboard: kpis exactly; do not recompute its counts, RAGs, deltas, or summaries.
- Funnel: pipeline_funnel exactly, including status labels, months, counts, percentages, deltas, and direction.
- PSI history and score distribution: psi_history and score_buckets exactly.
- Feature stability: features exactly, and the supplied ranking top_3_unstable exactly. Do not recompute Top 3 or instability streaks.
- Disbursal trend: disbursal_trend exactly, including null deltas as — and double_risk_event.
- Risk/approval/bureau analysis: risk_segments, approval_methods, and bureau_depth_segments exactly.
- Alerts: alerts exactly; render metric, current_value, threshold, status, persistent, and recommended_action as supplied.

CC PSI is informational only: never flag, badge, or change its severity. CC CSI values are reference only: no flag, badge, colour, or commentary about their level. Do not create extra alerts or change any supplied severity. Render alert statuses exactly as supplied. A 0% value does not prove absence unless that row explicitly has absent_in_disbursed=true. Do not infer, repair, or replace absent flags.

5. PRESENTATION AND INSIGHT LIMITS
Preserve raw labels verbatim, including feature names, segment names, method names, score-bucket labels, RAG/status names, threshold text, and alert text. Do not rename labels or introduce synonyms. Keep top-of-report insights concise and grounded only in supplied JSON. Each insight must use this compact format:
Evidence: [exact label] | Value: [exact display value] | Severity: [supplied RAG/status, or —]
Do not turn a correlation or co-occurrence into a cause. Do not state why something happened, assign accountability, prescribe an owner, quantify financial impact, add a threshold, or assert a date unless that exact claim/value exists in JSON. Do not infer trend, risk, persistence, absence, conversion, or priority beyond explicit JSON fields. Use no value from another section to fill a gap.

6. FINAL SELF-CHECK
Before emitting, verify: response is inline HTML only (not a file/attachment/filename); raw HTML starts with <!DOCTYPE html> and ends with </html>; no markdown or JavaScript exists; all nine headings appear once and in order; all listed top-level keys are represented; null/missing values display as —; CC PSI/CSI exclusions hold; every alert status is unchanged; no extra alert, severity change, unsupported causal claim, recalculation, invented threshold, owner, financial impact, or date appears.
